## Imputation

The process of replacing missing values in a dataset with substituted values instead of removing the data.

For example, you might

* replace missing numbers with the mean of the column (mean imputation)
* replace missing categories with the most frequent value
* fit a regression model to the non-missing values and predict missing values (regression imputation)
