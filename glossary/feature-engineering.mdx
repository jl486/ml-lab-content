## Feature engineering

A process that involves finding the most useful features, creating new features from existing ones, and enriching raw data to make them give more information to the model.

For example, you may create a new feature called `profit` by subtracting `cost` from `revenue`.
