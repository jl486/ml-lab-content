## Categorical feature

A feature that can take a limited number of possible values. Categorical features can be <Link href="/glossary#ordinal-variable">ordinal</Link> or <Link href="/glossary#nominal-variable">nominal</Link>.

For example, a categorical feature called `transportation` can only have the values `bike`, `car`, `bus`, or `train`.
