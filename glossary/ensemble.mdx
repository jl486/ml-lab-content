## Ensemble

A technique that combines the predictions of multiple models to produce a final prediction that is more accurate and robust than a single model on its own.

The most common types of ensembles are bagging and boosting.
